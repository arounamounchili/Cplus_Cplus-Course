#include <iostream>

using namespace std;

int main()
{
    // Les opérateurs arithmetiques: +,-,*,/,%
    int x = 100 + 20;
    int y = 12 - 23;
    int z = x * y;
    int a = 12 / 2;
    int b = 13 % 3;

    // Les opérateurs de comparaisons: ==, <, <=, >, >=, !=
    cout << "x == y is " << (x == y) << endl;

    // Les opérateurs logiques: &&, ||, !
    cout << "x && y is " << (a && b) << endl;

    // Les opérateurs binaires: &,|, ^, <<, >>, ~,

    // Les opérteurs d'affectation; =, +=, -=, *=, /=

    // Les opérateurs terniaires: Expression1? Expression2: Expression3

    return 0;
}